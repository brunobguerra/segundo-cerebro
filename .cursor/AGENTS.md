---
description: 
alwaysApply: true
---

# Agents

Read **`CLAUDE.md` first**. Human-only trees are **`A*`** (capture) and **`C02`/`C03`/`C05`** (integrations); agent-maintained wiki lives under **`B01`**, **`B03`**, **`B05`** (and **`B04`** when archiving). Ingest is one source at a time with human confirmation; lint is on-demand; outputs are Markdown and Mermaid unless the human expands scope.
