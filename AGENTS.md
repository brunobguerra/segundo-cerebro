---
description: 
alwaysApply: true
---

# Agents

Read **`CLAUDE.md` first**. The same rules apply here: `raw/` is human-only (immutable for agents), `wiki/` is maintained by the LLM, ingest is one source at a time with human confirmation, lint is on-demand, outputs are Markdown and Mermaid unless the human expands scope.
